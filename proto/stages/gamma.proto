syntax = "proto3";

option go_package = "common/communication/server-comm/protocol";



message Gamma {
    message Data {
        string id = 1;
        repeated string prod_countries = 2;
        uint64 budget = 3;
    }

    repeated Data data = 1;
}