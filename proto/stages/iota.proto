syntax = "proto3";

option go_package = "common/communication/protocol";



message Iota {
    message Data {
        message Movie {
            string id = 1;
        }

        message Actor {
            string movieId = 1;
            string id = 2;
            string name = 3;
        }

        oneof data {
            Movie movie = 1;
            Actor actor = 2;
        }
    }

    repeated Data data = 1;
}